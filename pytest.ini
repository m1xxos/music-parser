[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests as integration tests (require network access)
    unit: marks tests as unit tests (no network access)
addopts = -m "not integration"
